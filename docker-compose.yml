services:
  finance-api:
    build: .
    image: household-finance-app:latest
    container_name: household-finance-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/finance.db
    volumes:
      - ./data:/data
